import type { Book } from "@/types/book"; import type { Collection } from "@/types/collection"; import { absoluteUrl } from "@/lib/site-url";
export function CollectionJsonLd({collection,books}:{collection:Collection;books:Book[]}){const url=absoluteUrl(`/collections/${collection.slug}`);const data={"@context":"https://schema.org","@type":"CollectionPage",name:collection.title,description:collection.seo.description,url,mainEntity:{"@type":"ItemList",numberOfItems:books.length,itemListElement:books.map((book,index)=>({"@type":"ListItem",position:index+1,name:book.title,url:absoluteUrl(`/books/${book.slug}`)}))}};return <script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(data).replace(/</g,"\\u003c")}}/>}
