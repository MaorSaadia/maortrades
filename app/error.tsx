"use client";
import { useEffect } from "react"; import { Button } from "@/components/ui/button";
export default function ErrorPage({error,unstable_retry}:{error:Error & {digest?:string};unstable_retry:()=>void}){useEffect(()=>{console.error("[app] route_error",{name:error.name,digest:error.digest})},[error]);return <div className="mx-auto max-w-3xl px-5 py-24 text-center"><p className="eyebrow text-gold">TEMPORARY ERROR</p><h1 className="heading-lg mt-4 text-navy">THIS PAGE COULD NOT BE DISPLAYED.</h1><p className="body-lg mt-5 text-muted-foreground">Try loading it again. Checkout, email, and purchase access should be retried only through their normal controls.</p><div className="mt-8 flex justify-center gap-3"><Button onClick={unstable_retry}>Try Again</Button><Button href="/" variant="outline">Return Home</Button></div></div>}
